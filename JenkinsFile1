currentBuild.displayName = "TEST BUILD"+currentBuild.number
pipeline {
    agent any
    environment {
        sh "sh set_env.sh"
    }
    stages {
        stage ('Hello') {
            steps {
                echo 'Hello World'
                }
             }
        stage ('Set Environment') {
            steps {
                echo "${var1}"
                sh "echo `pwd`"
                }
            }
    }
}
